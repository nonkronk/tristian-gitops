apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: promax1-certificate
  namespace: external
spec:
  secretName: promax1-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - ${SECRET_PROMAX1_DOMAIkN}
