apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: external
spec:
  ports:
    - name: default
      port: 80
      protocol: TCP
      targetPort: 80
  clusterIP: None
  type: ClusterIP
