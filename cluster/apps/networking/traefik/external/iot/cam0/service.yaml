apiVersion: v1
kind: Service
metadata:
  name: cam0
  namespace: external
spec:
  ports:
    - name: default
      port: 8000
      protocol: TCP
      targetPort: 8000
  clusterIP: None
  type: ClusterIP
