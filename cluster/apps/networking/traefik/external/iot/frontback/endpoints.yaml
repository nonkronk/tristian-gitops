apiVersion: v1
kind: Endpoints
metadata:
  name: frontback
  namespace: external
subsets:
  - addresses:
      - ip: 10.40.0.15
    ports:
      - name: default
        port: 80
        protocol: TCP
