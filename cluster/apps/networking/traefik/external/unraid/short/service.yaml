apiVersion: v1
kind: Service
metadata:
  name: short
  namespace: external
spec:
  ports:
    - name: default
      port: 3001
      protocol: TCP
      targetPort: 3001
  clusterIP: None
  type: ClusterIP
